db:
  image: dockerfile/mongodb
  ports:
    - "21017"
web:
  build: ./
  ports:
    - "3000:3000"
  links:
    - db
  environment:
    NODE_ENV: production
    HOSTNAME: ny.sundhedshjoernet.dk
    PORT: 3000
    IP: '127.0.0.1'
  volumes:
    - .:/code
  command: npm start
