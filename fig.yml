db:
  image: dockerfile/mongodb
  ports:
    - "27017:27017"
web:
  build: ./
  ports: 
    - "3000:3000"
  links:
    - db
  environment:
    NODE_ENV: development
  volumes:
    - .:/code
  command: npm start
